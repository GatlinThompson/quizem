<p style="color: green"><%= notice %></p>

<%= render @quiz %>
<hr>

<div class="d-flex justify-content-center">
  <%= link_to "Edit Quiz", edit_quiz_path(@quiz), class: "fs-4 pe-4" %>
  <%= link_to "Back to Quizzes", quizzes_path, class: "fs-4 ps-4" %>
</div>
<div class="d-flex justify-content-center">
  <%= button_to "Delete Quiz", @quiz, method: :delete, class: "col btn btn-danger fs-5 mt-3" %>
</div>
